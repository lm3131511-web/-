global:
  scrape_interval: 15s

scrape_configs:
  - job_name: "codex"
    metrics_path: /metrics
    static_configs:
      - targets:
          - "codex-app:8000"
        labels:
          service: "codex"
